# Architecture

> Auto-generated by /map on 2026-04-26

## Overview

Brightlight Immigration Landing Pages is a React-based web application built with Vite. It serves as a collection of high-conversion landing pages for various Canadian immigration programs (Franco, PGWP, Spousal, VOWP). The project emphasizes high-quality UI/UX using Tailwind CSS and Framer Motion for animations.

```
┌─────────────────────────────────────────┐
│              [main.jsx]                 │
├─────────────────────────────────────────┤
│                [App.jsx]                │
├─────────────────────────────────────────┤
│         [Pages / Components]            │
├─────────────────────────────────────────┤
│            [Static Data]                │
└─────────────────────────────────────────┘
```

## Components

### Layout Components
- **Navbar.jsx**: Sticky navigation bar with logo and CTA.
- **Footer.jsx**: Comprehensive footer with site links and contact info.
- **ServicePageLayout.jsx**: A reusable layout wrapper for program-specific landing pages.

### UI Components
- **Hero.jsx**: Impactful header section for landing pages.
- **MultiStepForm.jsx**: A complex lead generation form with multiple steps.
- **ProcessSteps.jsx**: Visual representation of the immigration process.
- **FAQ.jsx**: Accordion-style frequently asked questions.
- **Testimonials.jsx** & **GoogleReviews.jsx**: Social proof sections.
- **MeetLoveneet.jsx**: Personal branding section for the consultant.
- **ReelsSection.jsx**: Video content integration (Instagram/TikTok style).

## Data Flow

1. **Entry Point**: `main.jsx` renders `App.jsx`.
2. **Routing**: `App.jsx` likely handles routing (though `react-router-dom` wasn't in `package.json`, it might be handled differently or uses a simple conditional render). *Correction*: I should check how pages are rendered.
3. **Content Injection**: Data from `src/data/services.js` and `sharedContent.js` is imported into pages and components to populate content dynamically based on the service.

## Integration Points

| Service | Type | Purpose |
|---------|------|---------|
| Instagram/Video | Embed | Video content for ReelsSection |
| Google Maps/Reviews | Widget | Social proof via GoogleReviews |

## Technical Debt

- [ ] **Routing**: Ensure a robust routing solution if more pages are added (currently seems to be a single-page or multi-entry setup).
- [ ] **API Integration**: Currently purely static; lead form (`MultiStepForm`) likely needs a backend integration or email service connection.
- [ ] **Legacy Content**: `legacy-html/` directory contains old files that could be fully migrated or removed.

## Conventions

**Naming:** Component files use PascalCase (`FAQ.jsx`, `Navbar.jsx`).
**Structure:** Separated into `pages`, `components`, and `data`.
**Styling:** Utility-first styling with Tailwind CSS, customized in `tailwind.config.js`.
